---
id: B-008
title: Watchdog semantics: PT5M idle-silent abort fires during long model/API hangs, and the PT30M ticket budget kills BUSY sessions
type: bug
status: product-backlog
priority: high
role: developer
stage: implementation
model: null
story_points: 3
sprint: null
epic: null
assignee: null
blocked: false
blocker: null
labels: ["fleet","reliability","watchdog"]
acceptance_criteria: ["The stall abort records a diagnostic snapshot on the ticket: last assistant text age, last tool call (name + age), pending request state - visible in the blocker text","The per-ticket run budget is progress-aware: observed progress (new messages/tool activity) resets or extends the budget window; a continuously busy session is never budget-killed","A genuinely hung session (no progress for the full window) still aborts and blocks with the reason","Engine tests pin progress-reset vs no-progress-abort and the diagnostic snapshot"]
created_at: 2026-09-20T16:55:37.603Z
updated_at: 2026-09-23T05:35:44.657Z
---

Live 2026-09-20 (wave run): ten workers aborted with 'stalled: session idle and silent for PT5M' - the primary trigger was the missing task_* tools spiral (fixed: worktree-aware launchers), but the detector itself needs a second look: (1) a worker in a long silent MODEL/API phase (rate-limited provider, huge prompt, hung HTTP with no stream deltas) reads as 'idle and silent' and dies without any diagnostic of what it was waiting on - the abort should record the last observed state (last tool call, pending request age) so the next triage distinguishes model-hang from tool-hang from true-hang; (2) U-024 was killed by the PT30M ticket budget while busy:true with 50 messages and active test work - a budget that resets (or extends) on observed progress would not kill healthy long runs; the watchdog should abort on NO-progress, never on progress. Pairs with B-004 (timeout path leaks the serve).

## Comments
{"ts":"2026-09-20T17:12:50.083Z","by":"chat-session","text":"Live instance (2026-09-20, wave run): the B-007 requirements worker COMPLETED its stage (final assistant message was the completion report) yet the session was never detected complete - 5 min later the idle-silent watchdog aborted it as \u0027stalled\u0027 and the settle never merged the (uncommitted) deliverable. Two gaps in one: (a) completion detection did not fire on an idle session whose last message is an assistant reply WITH text (v2 reply-polling semantics - check the isComplete heuristic against v2 message/state shapes), (b) the stall abort then fired on a finished session. Same family seen on B-005/B-007 2026-09-20 evening run."}
{"ts":"2026-09-22T04:09:06.922Z","by":"pm","text":"Live evidence 2026-09-22 (T-001 demo): session went idle with outcome\u003dsucceeded, yet fleet_jobs kept RUNNING for ~7 minutes until the watchdog settled. Completion detection is far too slow even in the success path - the probe saw idle+complete long before the settle. Profile the probe/complete criteria before the next wave."}
{"ts":"2026-09-23T05:34:50.127Z","by":"kimi-k3","text":"Watchdog redesigned (AC1-AC4) with the rubberduck findings folded in: (F-1) HttpOpencodeClient.getMessages now normalizes to chronological order in ONE choke point (the wire is newest-first per the mirrored capture; every consumer works chronologically - the fakes masked the contradiction). (F-2/F-3) ONE completion judge (com.opencode.ide.client.model.Turns): the v2 idle turn-end marker is the authoritative turn end (settles even with a stuck prompt POST - kills the ~7-minute success-path lag), the finished-reply evidence (trailing conversational entry, assistant text, time.completed stamp, no in-flight tool/shell) additionally requires the prompt POST resolved; awaitReply uses the same judge (its old early-stamp return force-settled the watchdog mid-turn - F-005\u0027s root). (F-4) The budget is a NO-PROGRESS window (message/streaming-growth/tool-activity/busy reset it; busy sessions never budget-killed per AC2) with FleetTuning.HARD_RUN_CAP as the absolute backstop (slot starvation/cost runaway); permission asks pause the stall clock but not the budget (F1 preserved: \u0027dies if nobody ever answers\u0027). (AC1) Every abort carries the diagnostic snapshot (last assistant text + age, last tool call + age, pending request state) into the FAILED detail and the blocker. Tests: the busy/progress flips pin the new contract, plus marker-settles-stuck-POST, trailing-marker-does-not-mask-completion (gap a), streaming-until-stamped (F-005 root), no-progress-budget + snapshot pins. Docs aligned (AGENTS.md, FleetToolProvider tool docs)."}

## History
{"ts":"2026-09-20T16:55:37.603Z","action":"created"}
{"ts":"2026-09-23T05:35:44.657Z","action":"updated:stage"}
