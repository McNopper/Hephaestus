---
id: U-002
title: GUI right-click batches A/B/C (from U-001 + 2026-09-13 evaluation)
type: task
status: product-backlog
priority: low
role: developer
stage: null
story_points: 8
sprint: null
epic: null
assignee: null
blocked: false
blocker: null
labels: ["ui","menus","backlog"]
acceptance_criteria: ["Batch A implemented per the matrix when scheduled","Delete/abort confirmations follow the tier policy","Fleet reliability tickets (F-*) take precedence over this work"]
created_at: 2026-09-13T17:18:27.909Z
updated_at: 2026-09-16T08:21:45.861Z
---

Backlog the full right-click evaluation (2026-09-13, extends U-001). Full matrix delivered in-session; highlights: delete session needs one new client method (DELETE /session/:id documented since our pin); abort session needs only menu items (client exists); new-session-with-agent needs an openChat agentId param + ChatView.preselectAgent; model-half already ships. Batches: A=quick wins (Board row menu: Launch/Take over/Open ticket; FleetView row menu; copy-path/id everywhere; agent details dialog), B=new client surface (deleteSession + Delete action with tier-2 confirm; abort actions; agentId param; message-scoped revert/fork), C=deferred (rename, open-in-editor, MCP details). Confirmation policy: tier-0 none / tier-1 one-click / tier-2 identity+gates for delete (abort busy first, subagent-cascade warning, non-RUNNING only in FleetView). Priority: BELOW fleet reliability work (user directive 2026-09-13).

## Artifacts
{"kind":"path","ref":"eclipse/bundles/com.opencode.ide.ui + com.opencode.ide.ui.tests (Batch C: McpServerRows, SessionTranscript, StringStorageEditorInput; ServerView, SessionDetailsView, ServerSelection; tests McpServerRowsTest, SessionTranscriptTest, StringStorageEditorInputTest, ServerSelectionTest, ServerMenuSmokeTest)","note":"Batch C shipped in the ui/ui.tests bundles only: (2) Open-in-editor — new SWT-free SessionTranscript (message/transcript plain-text rendering) + read-only StringStorageEditorInput (IStorageEditorInput over a string), wired as \"Open message in Editor\" / \"Open transcript in Editor\" context-menu actions in SessionDetailsView (tier-0, no confirmation; opens org.eclipse.ui.DefaultTextEditor by id, no new bundle dependency). (3) MCP details — new SWT-free McpServerRows (sorted rows + dialog text; shows name + STATUS, not transport: the pinned 1.18.30 GET /mcp wire shape is {name:{status}} only), \"MCP servers…\" context-menu action in ServerView fed from the node\u0027s already-loaded list, enablement via new ServerSelection.mcpDetails(). (1) RENAME SKIPPED — no client surface and no endpoint evidence (see ticket comment). Tests: McpServerRowsTest, SessionTranscriptTest, StringStorageEditorInputTest (new), ServerSelectionTest + ServerMenuSmokeTest (extended); ui.tests MANIFEST gains org.eclipse.core.resources.","by":"executor (Batch C)","ts":"2026-09-16T08:21:20.402Z"}

## Comments
{"ts":"2026-09-16T08:21:45.861Z","by":"executor (Batch C)","text":"Batch C completion report (2026-09-16). SHIPPED: (2) Open-in-editor — SessionDetailsView context menu gains \"Open message in Editor\" (selected MessageRow) and \"Open transcript in Editor\" (full snapshot); content built SWT-free by new SessionTranscript (exact formats unit-tested), served through new StringStorageEditorInput (read-only IStorageEditorInput over a string, UTF-8, identity-equal so every open is a fresh snapshot) to org.eclipse.ui.DefaultTextEditor opened BY ID (registry lookup — no new Require-Bundle). Tier-0, no confirmation. (3) MCP details — ServerView context menu gains \"MCP servers…\" (enabled via new ServerSelection.mcpDetails(): any reachable server, incl. the root row and remote roots; offline remotes disabled); MessageDialog body built SWT-free by new McpServerRows from the ServerNode\u0027s already-loaded mcpServers list — NO new client call, tier-0. SKIPPED: (1) session RENAME — OpencodeClient has no title-update method (title only exists on POST /session create; the sole PATCH in HttpOpencodeClient is /config) and a repo-wide grep finds no session-title endpoint we already call; per the ticket policy endpoints are not guessed against the 1.18.30 pin. Revisit when a client method lands after re-verifying the server spec. DELIBERATELY MINIMAL: dialog is a MessageDialog (matches the existing agent-details dialog pattern) rather than a table; rows show name + status, NOT transport type — the pinned server\u0027s GET /mcp returns {name:{\"status\":…}} only (documented in McpServerRows); transcript editors are non-live snapshots (re-open to refresh). VERIFICATION (no Maven/Tycho run per instructions): full standalone javac compile of the client+core+ui+ui.tests bundles (117 files) against the deployed Eclipse SDK jars — clean; plain JUnitCore: 198/198 headless tests incl. 20 new (McpServerRowsTest 6, SessionTranscriptTest 9, StringStorageEditorInputTest 3, ServerSelectionTest +1, plus regressions) and the opt-in ServerMenuSmokeTest passed live with the new MCP-servers enablement assertions (primary root/session true, multi-select false). Files: ui — McpServerRows.java, SessionTranscript.java, StringStorageEditorInput.java (new), ServerSelection.java, ServerView.java, SessionDetailsView.java (edited); ui.tests — McpServerRowsTest.java, SessionTranscriptTest.java, StringStorageEditorInputTest.java (new), ServerSelectionTest.java, ServerMenuSmokeTest.java (edited), MANIFEST.MF (+org.eclipse.core.resources). Untouched by me: V-006.md / _design-v006-fleet-daemon.md changes pre-exist in the worktree."}

## History
{"ts":"2026-09-13T17:18:27.909Z","action":"created"}
{"ts":"2026-09-16T08:21:20.402Z","action":"artifact:path:eclipse/bundles/com.opencode.ide.ui + com.opencode.ide.ui.tests (Batch C: McpServerRows, SessionTranscript, StringStorageEditorInput; ServerView, SessionDetailsView, ServerSelection; tests McpServerRowsTest, SessionTranscriptTest, StringStorageEditorInputTest, ServerSelectionTest, ServerMenuSmokeTest)","by":"executor (Batch C)"}
