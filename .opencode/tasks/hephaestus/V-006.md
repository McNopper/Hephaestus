---
id: V-006
title: Fleet daemon mode: engine outlives any client session
type: task
status: product-backlog
priority: high
role: developer
stage: null
story_points: 8
sprint: null
epic: null
assignee: null
blocked: false
blocker: null
labels: ["fleet","headless","architecture","milestone-V-followup"]
acceptance_criteria: ["Design note on the ticket: transport (TCP socket vs named pipe), discovery + pidfile, auth (reuse the generated-password path), orphan shutdown semantics, attach/detach contract for MCP clients","A detached daemon entry point exists and can be started/stopped headlessly from bash","A reconnecting client (chat session) sees running jobs from a previous session; disconnecting does not kill them","Auto-dispatch loop can run inside the daemon without Eclipse","Tests for lifecycle + attach semantics over the fake engine seam"]
created_at: 2026-08-27T01:14:24.241Z
updated_at: 2026-09-15T03:54:23.611Z
---

User direction 2026-08-26: "we can run in headless mode and can connect and disconnect at any time". Today the engine's lifecycle is bound to the client that spawned it: the stdio MCP server dies with its session and the shutdown hook kills the spawned opencode serve + in-flight jobs - disconnect mid-run kills the run (bookkeeping survives on the ticket). Make the headless engine a daemon: a detached process owning FleetControl (spawned server, jobs, permission queue), with attach/attach-less MCP access so any chat session or Eclipse instance can connect and disconnect freely while runs continue. Also the natural home for the Auto dispatch loop (currently Eclipse-only). Design first (socket/pipe protocol, pidfile, discovery, auth via the generated password, orphan cleanup), then implement. Post-Milestone-V (V-004) - do not start before the validation run lands.

## Comments
{"ts":"2026-09-15T03:54:23.611Z","by":"chat-session","text":"Live evidence for the daemon/attach design (2026-09-15): five tickets dispatched from a chat session\u0027s fleet MCP server (own FleetControl in the stdio server process) are invisible to the Eclipse Fleet view (own FleetControl) - the operator cannot see or control chat-launched runs from the IDE. Cross-engine serialization still worked (DispatchGuard markers, store claims) - only visibility/lifecycle is split. Interim read-only visibility ticketed separately as F-004 (reconstruct job list from store claims + .git/opencode-fleet worktrees)."}

## History
{"ts":"2026-08-27T01:14:24.241Z","action":"created"}
