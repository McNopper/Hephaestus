---
id: V-006
title: Fleet daemon mode: engine outlives any client session
type: task
status: product-backlog
priority: high
role: developer
stage: null
story_points: 8
sprint: null
epic: null
assignee: null
blocked: false
blocker: null
labels: ["fleet","headless","architecture","milestone-V-followup"]
acceptance_criteria: ["Design note on the ticket: transport (TCP socket vs named pipe), discovery + pidfile, auth (reuse the generated-password path), orphan shutdown semantics, attach/detach contract for MCP clients","A detached daemon entry point exists and can be started/stopped headlessly from bash","A reconnecting client (chat session) sees running jobs from a previous session; disconnecting does not kill them","Auto-dispatch loop can run inside the daemon without Eclipse","Tests for lifecycle + attach semantics over the fake engine seam"]
created_at: 2026-08-27T01:14:24.241Z
updated_at: 2026-09-16T07:59:27.322Z
---

User direction 2026-08-26: "we can run in headless mode and can connect and disconnect at any time". Today the engine's lifecycle is bound to the client that spawned it: the stdio MCP server dies with its session and the shutdown hook kills the spawned opencode serve + in-flight jobs - disconnect mid-run kills the run (bookkeeping survives on the ticket). Make the headless engine a daemon: a detached process owning FleetControl (spawned server, jobs, permission queue), with attach/attach-less MCP access so any chat session or Eclipse instance can connect and disconnect freely while runs continue. Also the natural home for the Auto dispatch loop (currently Eclipse-only). Design first (socket/pipe protocol, pidfile, discovery, auth via the generated password, orphan cleanup), then implement. Post-Milestone-V (V-004) - do not start before the validation run lands.

## Artifacts
{"kind":"doc","ref":".opencode/tasks/hephaestus/_design-v006-fleet-daemon.md","note":"V-006 AC1 design note (kind\u003ddesign): transport, discovery+pidfile, auth, orphan semantics, attach/detach contract, auto-dispatch in the daemon, rollout slices, open questions. Grounded in FleetControl/FleetStdioMain/DispatchGuard/OpencodeServerLauncher/fleet-tools.ps1. Condensed decisions also recorded as ticket comment 2026-09-16.","by":"chat-design-session","ts":"2026-09-16T07:59:27.322Z"}

## Comments
{"ts":"2026-09-15T03:54:23.611Z","by":"chat-session","text":"Live evidence for the daemon/attach design (2026-09-15): five tickets dispatched from a chat session\u0027s fleet MCP server (own FleetControl in the stdio server process) are invisible to the Eclipse Fleet view (own FleetControl) - the operator cannot see or control chat-launched runs from the IDE. Cross-engine serialization still worked (DispatchGuard markers, store claims) - only visibility/lifecycle is split. Interim read-only visibility ticketed separately as F-004 (reconstruct job list from store claims + .git/opencode-fleet worktrees)."}
{"ts":"2026-09-16T07:59:16.381Z","by":"chat-design-session","text":"DESIGN NOTE (AC1) — full version: .opencode/tasks/hephaestus/_design-v006-fleet-daemon.md. Key decisions:\n\nTRANSPORT — TCP socket on 127.0.0.1, ephemeral port advertised in the pidfile; same line-based JSON-RPC as the stdio server (reuse McpDispatcher.handle per line, so the stdio proxy is a verbatim line pump). Named pipe REJECTED for MVP: pure Java cannot *serve* a Windows pipe (no CreateNamedPipe in the JDK) — would need JNI/external helper. Loopback-only; TLS/multi-machine deferred.\n\nDISCOVERY+PIDFILE — \u003crepo\u003e/.git/opencode-fleet/daemon.json, one daemon per repo. Location wins the evaluation: inherits per-repo scoping (engines/markers/claims are already repo-scoped), joins the established cross-engine coordination dir (admission.lock, .dispatch markers, worktrees), lives outside StoreSync\u0027s git path (zero noise), and reuses the proven pid+start-instant liveness convention of DispatchGuard markers. Pidfile also records serverPid/serverPort of the lazily-spawned opencode serve so a successor can kill an orphaned server. Cons (accepted): re-clone wipes it; main-worktree assumption (same as GitWorktreeManager).\n\nAUTH — reuse the generated-password path: daemon generates at start via FleetControl.resolvePassword (FLEET_DAEMON_PASSWORD env wins), publishes the password IN daemon.json. A second client learns it by reading the pidfile — the trust boundary is filesystem access to .git/opencode-fleet/ (a writer there can forge markers anyway; the password only stops drive-by port scans). Handshake: first line must be authed daemon/hello or the socket closes. Never logged; rotation \u003d restart.\n\nLIFECYCLE+ORPHANS — start via eclipse/fleet-daemon.ps1 start (detached Start-Process; java FleetDaemonMain); engine stays lazy (first dispatch spawns opencode serve, backfills serverPid into the pidfile). Graceful stop \u003d authed daemon/shutdown → FleetControl.close() (existing R3 drain-then-kill: 30s grace, then cancel + kill server tree) — NOTE: graceful stop DOES end in-flight runs; \"disconnect doesn\u0027t kill runs\" means CLIENT detach. Crash recovery reuses existing machinery (DispatchGuard.sweepStale dead-pid markers, F-002 reconcileOrphanedClaims) + new: successor kills the recorded serverPid if alive. Store stays the truth; job map is a projection; watchdog (5min stall) unchanged.\n\nATTACH/DETACH — FleetStdioMain gains modes via FLEET_DAEMON env: off (today\u0027s own-engine; initial default), auto (live daemon → thin proxy, else local engine), always (proxy or error). Proxy \u003d pidfile → connect → daemon/hello → pump lines; identical tool surface, so fleet_jobs from a reconnecting session shows daemon runs (AC3) and fleet_auto_* drives the daemon\u0027s loop (AC4). Non-exclusive by design: peers keep their own FleetControl; DispatchGuard serializes cross-engine dispatch; cross-engine visibility stays F-004\u0027s store-based view. New tools: fleet_daemon_status, fleet_daemon_shutdown (start stays a script action — no bootstrap from inside a session).\n\nAUTO-DISPATCH — no code motion: FleetControl.startAuto already hosts DispatchScheduler SWT-free on a daemon thread; inside the daemon JVM the loop outlives clients by construction. fleet_auto_stop from any attached client stops the daemon\u0027s (single) loop — documented single-owner semantic.\n\nROLLOUT — slice 1: FleetDaemonMain/FleetDaemon/DaemonPidfile + fleet-daemon.ps1 + lifecycle tests over the fake-engine seam (FleetControl(Path,Function\u003cPath,Engine\u003e)); slice 2: proxy mode + daemon tools + attach tests (two sequential attaches see the same job map; daemon death exits non-zero); slice 3: flip default off→auto ONLY after the V-004 validation run + AGENTS.md H7 update. opencode.json unchanged; store format, DispatchGuard protocol, StoreSync untouched. Linux: pure-JDK design runs as-is (setsid/nohup); later refinement \u003d Unix-domain socket behind the same transport seam.\n\nOPEN QUESTIONS — (1) direct streamable-HTTP MCP exposure vs stdio proxy (defer); (2) idle auto-exit knob FLEET_DAEMON_IDLE_EXIT_MS; (3) permission-ask push notifications; (4) fleet-daemon.ps1 list helper for multi-repo; (5) Windows Job Object hardening vs accept successor-cleanup; (6) Eclipse Fleet view auto-attach (separate UI ticket)."}

## History
{"ts":"2026-08-27T01:14:24.241Z","action":"created"}
{"ts":"2026-09-16T07:59:27.322Z","action":"artifact:doc:.opencode/tasks/hephaestus/_design-v006-fleet-daemon.md","by":"chat-design-session"}
