---
id: B-002
title: Board does not reflect store changes written by peer agents (new sprint invisible)
type: bug
status: done
priority: medium
role: developer
stage: null
story_points: 3
sprint: null
epic: null
assignee: null
blocked: false
blocker: null
labels: ["board","ux","live-found","multi-peer"]
acceptance_criteria: ["Reproduce with a peer write: while the Board is open, another agent (chat tasks server or file edit) changes the store (ticket status, new sprint) - the Board reflects the change without manual reload, or clearly indicates staleness with a Refresh affordance","When a new sprint is created by a peer, the Board\u0027s sprint selector lists it on the next refresh and the view does not silently show an empty/unchanged board (auto-select if the current selection is empty/none)","Watcher events from git operations (commit/checkout replacing files) are handled - not just in-place edits","The store the Board watches is the adopted repo store (ties into O-001 tasksRoot derivation), not a stale preference default"]
created_at: 2026-09-15T03:54:05.420Z
updated_at: 2026-09-16T07:18:59.021Z
---

User report 2026-09-15 (live session): after the chat session planned sprint-2026-09-15, created tickets and moved five tickets through claim/dispatch, the open Board view in Eclipse did not update. The Board already has a TaskStoreWatcher + single-flight refresh + polling (BoardView.java:142) and a manual refresh action, so the gap is somewhere in: watcher not firing for peer writes (the tasks stdio server / fleet workers write the same files), the watched root being a stale preference default instead of the adopted repo store (see O-001 AC-1), or the sprint selector not surfacing a peer-created sprint so the view looks unchanged. Diagnose which, fix, and make peer-written state visible by default - the store is a multi-peer blackboard (AGENTS.md) and the Board is its projection.

## Artifacts
{"kind":"path","ref":"eclipse/bundles/com.opencode.ide.board{,.tests}/src/main/java/com/opencode/ide/board/{model/TaskStoreWatcher.java,model/SprintSelection.java,model/TasksRootResolution.java,views/BoardView.java}","note":"B-002 peer-visibility fix: TaskStoreWatcher fingerprint now content-sensitive (CRC32) so peer/git rewrites that keep name+size+mtime still fire; new SWT-free SprintSelection auto-selects the newest sprint when the unpicked (backlog) board is empty (explicit picks never overridden — BoardView sprintPicked); new TasksRootResolution makes the watched root adopt the repo store (workspace climb, then open-workspace projects via OpencodeConnection.repoRootOf) with the tasksRoot preference demoted to a fallback below adoption, and the preference→dialog-settings freeze in loadSettings removed. Tests: TaskStoreWatcherTest peer suites, SprintSelectionTest, TasksRootResolutionTest, BoardModelTest peer-sprint test.","by":"executor (board half)","ts":"2026-09-16T07:06:40.818Z"}

## Comments
{"ts":"2026-09-16T07:07:21.903Z","by":"executor (board half)","text":"COMPLETION REPORT — B-002 (board bundle). Diagnosis + fix of all three suspected gaps; all three were real to varying degrees.\n\nDIAGNOSIS:\n(a) Watcher: WatchService + 2s poll already see cross-process writes, BUT the poll fingerprint keyed only (name, mtime, size) — a peer rewrite landing in the same millisecond with identical byte size (git checkout/rebase shapes) was invisible if the OS event was missed. Hardened to include a CRC32 content checksum (stores are a few hundred KB — negligible cost).\n(b) Sprint selector: refreshSprintCombo already lists every sprint the (uncached, always-fresh) TaskStore read yields, BUT when the board sat on the default (backlog) pseudo-sprint and a peer planned a sprint + moved the tickets into it, the board silently rendered EMPTY — exactly the live report. Fixed with auto-select (see below).\n(c) Watched root: resolveTasksRoot ranked a non-blank preference value ABOVE repo adoption, and loadSettings additionally copied the preference into the persisted dialog-settings override (rootOverride) — freezing whatever the preference said forever (the B-002 \"stale preference default\"; P1-4 removed the old dev default but workspaces kept the frozen copy). This is the primary live-failure path: the board watched a store no peer wrote to, and even manual Refresh read the stale root.\n\nFIXES:\n1. TaskStoreWatcher: fingerprint \u003d name+mtime+size+CRC32(content).\n2. NEW model/SprintSelection (SWT-free, tested): auto-select the newest real sprint ONLY when the user hasn\u0027t picked one this session (BoardView.sprintPicked, set by the combo listener, reset on project/root change), the current selection is (backlog), and that selection\u0027s board is empty. BoardView.maybeAutoSelectSprint applies it after each snapshot (cancels the dispatch loop like a manual pick, status line explains \"Auto-selected sprint X — (backlog) was empty\", triggers a re-render).\n3. NEW model/TasksRootResolution (SWT-free, tested): order \u003d explicit toolbar/dialog override (honored even when missing — the mistake stays VISIBLE as the not-found notice) → workspace-upward climb → open-workspace-projects repo adoption (reuses the PUBLIC OpencodeConnection.repoRootOf marker-climb rule — same \"same folder level\" parity as the spawned server; board MANIFEST gains org.eclipse.core.resources, same style as core\u0027s) → tasksRoot preference as fallback BELOW adoption → historical ../.opencode/tasks guess. loadSettings no longer seeds rootOverride from the preference. The static BoardView.tasksRoot() seam (FleetView peer scan) inherits the same resolution.\n4. BoardModelTest proves a second TaskStore instance (the peer) planning a sprint shows up in model.sprints() on the next call (no caching anywhere).\n\nAC MAPPING: AC1 peer write → watcher fires (test: TaskStore write + out-of-band peer replace/delete); AC2 selector lists + auto-select (tests: peerCreatedSprintAppearsInTheSprintListOnRefresh + SprintSelectionTest suite); AC3 git-replace handled (test: firesWhenPeerReplacesContentPreservingSizeAndMtime — delete+recreate with identical size AND restored mtime; note the OS also emits watch events for that shape, so the test proves the behavior end-to-end while the checksum is defense-in-depth for silently-lost events); AC4 adopted repo store (tests: openProjectAdoptionBeatsTheStalePreferenceDefault + workspaceClimbBeatsAdoptionAndPreference + TasksRootResolutionTest rest).\n\nTESTS ADDED: firesWhenPeerReplacesContentPreservingSizeAndMtime, firesWhenPeerDeletesATicket (TaskStoreWatcherTest); NEW SprintSelectionTest (7 methods); NEW TasksRootResolutionTest (11 methods); peerCreatedSprintAppearsInTheSprintListOnRefresh (BoardModelTest).\n\nKNOWN LIMITS (reported, not silently dropped): a dialog-settings override frozen by the OLD seeding on an existing workspace still wins until the user clears the Store field (explicit overrides are honored by design — flipping them automatically would fight the user; the preference itself now re-evaluates live). Maven/Tycho not run (orchestrator verifies centrally). Confidence: high on model/watcher logic (tested); medium-high on the live-workspace healing until verified in the IDE."}

## History
{"ts":"2026-09-15T03:54:05.420Z","action":"created"}
{"ts":"2026-09-16T07:06:40.818Z","action":"artifact:path:eclipse/bundles/com.opencode.ide.board{,.tests}/src/main/java/com/opencode/ide/board/{model/TaskStoreWatcher.java,model/SprintSelection.java,model/TasksRootResolution.java,views/BoardView.java}","by":"executor (board half)"}
{"ts":"2026-09-16T07:18:59.021Z","action":"updated:status"}
