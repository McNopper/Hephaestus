---
id: U-007
title: Show live agent-working (busy) state for sessions in the GUI
type: story
status: done
priority: medium
role: developer
stage: null
story_points: 3
sprint: null
epic: null
assignee: null
blocked: false
blocker: null
labels: ["sessions","server-view","ux","visibility"]
acceptance_criteria: ["Sessions with an actively working agent show a live busy state in the Session/Server view (busy icon - session-busy.png exists in the ui bundle - or equivalent), refreshed from the server\u0027s busy-session status while the view is open","The busy state appears and clears without manual refresh while the agent works (event- or poll-driven, off the UI thread)","The chat view\u0027s own session shows in-flight state (Stop control / thinking indicator from U-003 already covers the chat side - link them to the same source of truth where practical)","Fleet worker sessions surface their busy state too (same mechanism), so \u0027an agent is working\u0027 is answerable at a glance from the GUI"]
created_at: 2026-09-16T04:59:35.959Z
updated_at: 2026-09-16T07:18:59.094Z
---

User direction 2026-09-15 (wave-1 wrap-up): "in the gui, if an agent is working, on the session, this should be visible." Today the busy state of a session (chat reply streaming, fleet worker running) is not surfaced in the Session/Server view - the ui bundle already ships session.png and session-busy.png icons, so the visual language exists but the live state does not. Source of truth: the opencode server's session status (busy map; the same signal the fleet watchdog uses). Pairs with U-003 (chat-side thinking/Stop) and F-004 (peer jobs in the Fleet view).

## Artifacts
{"kind":"file","ref":"eclipse/bundles/com.opencode.ide.ui/src/main/java/com/opencode/ide/ui/session/SessionBusyPoller.java","note":"SWT-free poller (com.opencode.ide.ui.session) + canonical session icon keys + pure helpers toBusySet/mergeInto/iconKey","by":"executor (U-007)","ts":"2026-09-16T06:52:23.865Z"}
{"kind":"file","ref":"eclipse/bundles/com.opencode.ide.ui.tests/src/main/java/com/opencode/ide/ui/session/SessionBusyPollerTest.java","note":"JUnit 4, SWT-free: 13 tests — busy-set delivery, idle-on-absent, retry after failure + transition-only logging, dispose stops polling/joins thread, pure mapping helpers","by":"executor (U-007)","ts":"2026-09-16T06:52:23.960Z"}
{"kind":"file","ref":"eclipse/bundles/com.opencode.ide.ui/src/main/java/com/opencode/ide/ui/views/ServerView.java","note":"View wiring: per-client poller lifecycle (updateBusyPollers/onBusySessions/logBusyPollError), poller disposal in dispose(), session icon routed through SessionBusyPoller.iconKey, class javadoc updated","by":"executor (U-007)","ts":"2026-09-16T06:52:32.378Z"}
{"kind":"file","ref":"eclipse/bundles/com.opencode.ide.ui/src/main/java/com/opencode/ide/ui/internal/UiActivator.java","note":"ICON_SESSION/ICON_SESSION_BUSY now alias SessionBusyPoller\u0027s canonical keys (single source of truth)","by":"executor (U-007)","ts":"2026-09-16T06:52:32.456Z"}

## Comments
{"ts":"2026-09-16T06:31:53.971Z","text":"Progress 2026-09-16: chat-side in-flight visibility landed (Stop control + thinking state via the late-reply watcher work in 6ae0e73). Still open: Server/Session view busy icons (session-busy.png) refreshed from the server\u0027s busy map, and fleet worker session busy state. Board/Fleet icons unrelated to this ticket landed under U-006."}
{"ts":"2026-09-16T06:52:49.031Z","by":"executor (U-007)","text":"Completion report — Server/Session view live busy state (implementation + tests; NOT committed, worktree only; no Maven/Tycho run per orchestrator instruction).\n\nFILES (4):\n- NEW eclipse/bundles/com.opencode.ide.ui/src/main/java/com/opencode/ide/ui/session/SessionBusyPoller.java\n- NEW eclipse/bundles/com.opencode.ide.ui.tests/src/main/java/com/opencode/ide/ui/session/SessionBusyPollerTest.java\n- MOD eclipse/bundles/com.opencode.ide.ui/src/main/java/com/opencode/ide/ui/views/ServerView.java (+~110 lines)\n- MOD eclipse/bundles/com.opencode.ide.ui/src/main/java/com/opencode/ide/ui/internal/UiActivator.java (icon keys alias the poller\u0027s canonical constants)\n\nHOW IT WORKS:\n1) SessionBusyPoller (SWT-free, exported package ui.session, x-friends visible to ui.tests): constructed per OpencodeClient with DEFAULT_INTERVAL_MILLIS\u003d2000 and an error consumer; start() spawns one daemon thread that polls getSessionStatus() immediately then every interval, projects the map to the busy id set (type busy|retry; absent\u003didle per opencode 1.18.23 wire contract) and notifies listeners on the poller thread (view hops via Display.asyncExec — same pattern as the SSE listeners). Failed polls never clear the last set and are reported only on the transition into failure (one log line per outage); dispose() is idempotent, interrupts the worker, never blocks the UI (awaitTermination is test-only).\n2) ServerView: showNodes -\u003e updateBusyPollers(nodes) keeps exactly one poller per live connection client (offline roots skipped; a reconnect\u0027s new client retires the old poller). onBusySessions(client, busy) reconciles the set into every node sharing that client via pure mergeInto(): adds busy entries (preserving SSE-written \"retry\" markers), removes stale busy entries, leaves explicit idle entries alone — node.statuses stays the single busy source, so the EXISTING rendering (ServerLabels.isBusy -\u003e icon, sessions category \"n busy\" detail, agent-nested busy-first ordering) updates automatically; a coalesced scheduleRefresh() fires only when something changed. Fleet worker sessions need no special case: ordinary sessions in the same status map.\n3) Icon mapping is unit-testable: SessionBusyPoller.iconKey(active) returns the canonical ICON_SESSION/ICON_SESSION_BUSY keys (UiActivator aliases them); the view\u0027s icon() routes through it.\n\nTESTS ADDED (SessionBusyPollerTest, JUnit 4, SWT-free, 13 methods, all green):\n- toBusySetKeepsBusyAndRetryAndDropsEverythingElse, toBusySetToleratesNullAndEmptyMaps\n- mergeIntoAddsBusyEntriesAndPreservesRetryMarkers, mergeIntoClearsStaleBusyEntriesAndKeepsExplicitIdle, mergeIntoWithoutChangeReportsFalse, mergeIntoToleratesNullArguments\n- iconKeyMapsBusyToTheBusyIconAndIdleToTheIdleIcon\n- pollDeliversTheClientBusySet, busySetShrinksWhenASessionGoesIdle (absent \u003d idle), clientFailureDoesNotKillThePollerAndLogsOnlyTheTransition (fake fails 2x, retries, delivers; exactly 1 logged transition), disposeStopsPollingAndTerminatesTheThread (idempotent dispose, thread joined with timeout, no further callbacks), disposeBeforeStartPreventsAnyPolling, zeroIntervalIsRejected\n\nVERIFICATION: full ui bundle compiled clean with javac 21 against the client/core target classes + real platform jars from the local repo; tests run directly via JUnitCore: OK (13 tests) in 0.25s.\n\nSKIPPED / NOTES: no Maven/Tycho run (central verification per instructions); nothing committed. Chat-side (U-003) untouched — already covered. The SSE stream remains the more granular source (retry types, activity labels); the poller only backstops it, so no behavior regression if events flow. Known benign race (documented in code): a poll response generated just before an SSE idle event can re-assert busy for up to one interval; self-corrects on the next poll."}

## History
{"ts":"2026-09-16T04:59:35.959Z","action":"created"}
{"ts":"2026-09-16T06:52:23.865Z","action":"artifact:file:eclipse/bundles/com.opencode.ide.ui/src/main/java/com/opencode/ide/ui/session/SessionBusyPoller.java","by":"executor (U-007)"}
{"ts":"2026-09-16T06:52:23.960Z","action":"artifact:file:eclipse/bundles/com.opencode.ide.ui.tests/src/main/java/com/opencode/ide/ui/session/SessionBusyPollerTest.java","by":"executor (U-007)"}
{"ts":"2026-09-16T06:52:32.378Z","action":"artifact:file:eclipse/bundles/com.opencode.ide.ui/src/main/java/com/opencode/ide/ui/views/ServerView.java","by":"executor (U-007)"}
{"ts":"2026-09-16T06:52:32.456Z","action":"artifact:file:eclipse/bundles/com.opencode.ide.ui/src/main/java/com/opencode/ide/ui/internal/UiActivator.java","by":"executor (U-007)"}
{"ts":"2026-09-16T07:18:59.094Z","action":"updated:status"}
