---
id: T-004
title: v2/TUI parity: permission asks visible and answerable from the chat surface (with reconnect recovery)
type: story
status: product-backlog
priority: high
role: developer
stage: implementation
model: null
story_points: 5
sprint: null
epic: null
assignee: null
blocked: false
blocker: null
labels: ["parity","v2","chat"]
acceptance_criteria: ["A permission ask raised by a session appears in the chat surface within one poll interval with its display title/resources","The user answers from the chat surface (once / always / reject with optional feedback) through the single answer path POST /session/:id/permission/:requestID/reply - no second answer path is forked","Reconnect recovery: on UI reconnect pending asks are re-read via GET /permission/request (events are not replayed) and asked-but-unanswered asks surface again","fleet_permissions/fleet_permissions_answer semantics unchanged; chat and board views share one queue per session","Unit/component tests cover ask-\u003eanswer and the reconnect re-read"]
created_at: 2026-09-23T05:34:50.168Z
updated_at: 2026-09-23T07:38:31.760Z
---

The TUI surfaces permission asks with an answer flow (once/always/reject + feedback). The harness has the backend (PermissionQueue/FleetPermissionBridge/fleet_permissions*, ChatPermissionSink seam, and now the client's poll-based listPermissionRequests = GET /api/permission/request) and a board-side dialog (FleetPermissionsDialog), but NOTHING in the chat surface. Event streams are per-process and not replayed, so asks raised while no listener was attached are invisible - the reconnect recovery read path is part of this ticket (rubberduck F-7).

## Comments
{"ts":"2026-09-23T07:15:15.649Z","by":"kimi-k3","text":"Scope update (user 2026-09-23): the full chat-side ask surface is NOW in scope and being implemented this session - banner + once/always/reject + reconnect recovery, together with the global background-activity view."}
{"ts":"2026-09-23T07:21:19.158Z","by":"kimi-k3","text":"Implemented (2026-09-23): the ask surface lands as the \u0027Permission asks\u0027 pane of the new Background view (com.opencode.ide.ui.views.BackgroundView) - pending asks from ANY session with allow-once / allow-always / reject through the single answer path (client.respondToPermission), answering off the UI thread (FleetPermissionsDialog rule), lenient on older servers. REMAINING slice of this ticket: the in-chat banner (ask raised in the open chat shows inline) - it needs a permission-listener fan-out first (ChatPermissions is a single-slot sink and TaskFleetLauncher.CHAT_SINK currently owns it; the banner must multiplex, not override), plus the reconnect re-read hook on chat subscribe."}
{"ts":"2026-09-23T07:38:31.760Z","by":"kimi-k3","text":"IA verdict (rubberduck 2026-09-23): the banner must NOT wire ChatPermissions.setSink before a fan-out exists - it is single-slot last-set-wins and TaskFleetLauncher.CHAT_SINK owns it; a naive banner silently steals the fleet\u0027s ask feed. Build the multiplexer/shared asks model FIRST (event feed + reconnect re-read + one answer path; PermissionQueue already dedups - reuse it). Also blocking for the AC \u0027reject with optional feedback\u0027: client.respondToPermission has NO feedback field - decide the transport (follow-up steer/synthetic message vs API gap) before the answer flow hardens. The banner stays ANSWERABLE in place (not a jump-link)."}

## History
{"ts":"2026-09-23T05:34:50.168Z","action":"created"}
