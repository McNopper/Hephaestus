---
id: T-005
title: v2/TUI parity: background tasks surface - background a session, shell task list/output/reap, inbox steer|queue|cancel
type: story
status: in-review
priority: high
role: developer
stage: implementation
model: null
story_points: 5
sprint: null
epic: null
assignee: null
blocked: false
blocker: null
labels: ["parity","v2","chat"]
acceptance_criteria: ["A running session can be backgrounded from the chat surface (POST /session/:id/background) and continues while long tools run","Shell tasks are listable with live status (running/exited/timeout/killed), their captured output readable, finished tasks reapable","Queued prompts are manageable: list, deliver-as steer|queue, cancel (session inbox API)","The surface is directory-scoped (location[directory]) like every other v2 list, and degrades leniently on older servers (404/unsupported \u003d hidden, never an error dialog)","Unit/component tests cover the client surface against stub responses"]
created_at: 2026-09-23T05:34:50.214Z
updated_at: 2026-09-23T10:58:07.675Z
---

The TUI has Ctrl+B (background blocking session tools), a Shell tab (running commands, live output, kill) and queued-prompt management (Alt+Enter queue vs Enter steer). The harness now has the CLIENT surface (backgroundSession, listShellTasks/shellTaskOutput/removeShellTask, listInbox/updateInboxItem/cancelInboxItem - all v2 routes, lenient on older servers) but no UI. Note the hidden dependency (rubberduck F-7): session lists are directory-scoped on the shared service - the surface must scope like the rest of the harness.

## Comments
{"ts":"2026-09-23T07:15:15.680Z","by":"kimi-k3","text":"Scope update (user 2026-09-23): the background tasks surface is being implemented this session - shells table (list/output/reap), inbox (steer|queue|cancel), session backgrounding, all inside the new background-activity view + a chat toolbar action."}
{"ts":"2026-09-23T07:38:31.817Z","by":"kimi-k3","text":"IA verdict (rubberduck 2026-09-23): inbox rows belong in the ChatView composer queue row (they ARE prompts) - decide the action mapping BEFORE rendering (local unsent \u003d Edit/Remove/Fork; server-parked \u003d steer|queue|cancel); two look-alike queues with disjoint actions is a trap. Backgrounding \u003d one controller action, two invokers (chat toolbar + Agents-tree context menu). Shells pane keeps global ownership + gains a Session column/filter (per-session \u003d filter, not chat-local tabs) and an upgrade target: live output in an Eclipse Console per shell task instead of the output-dialog snapshot."}
{"ts":"2026-09-23T07:46:20.278Z","by":"kimi-k3","text":"COMPOSER QUEUE MAPPING DECIDED (must-not #4 - decision recorded BEFORE any inbox rendering): ONE queue row in the composer, entries tagged by source. (1) LOCAL UNSENT (this window\u0027s pending submissions): Edit / Remove / Fork - existing semantics unchanged. (2) SERVER-PARKED (session inbox, delivery steer|queue): Steer now (PATCH delivery\u003dsteer) / Deliver next (PATCH delivery\u003dqueue) / Cancel (DELETE). Order: server-parked first (what the server does next), then local (what this window sends next). SEND-TIME parity mapping: Send \u003d steer (TUI Enter), the adjacent Queue action \u003d POST /prompt with delivery\u003dqueue (TUI Alt+Enter) - this needs a delivery field on the client\u0027s prompt path, the one implementation prerequisite left in this ticket."}
{"ts":"2026-09-23T10:58:07.610Z","by":"kimi-k3","text":"Wishlist cross-check 2026-09-23: IMPLEMENTED (Send-to-Queue / Background actions + delivery plumbing + inbox steer/queue/cancel client). Full reactor green at verify -Pquality (27 modules, zero SpotBugs/CPD findings). Status was stale - moving to in-review; acceptance rides on the T-002 live pass."}

## History
{"ts":"2026-09-23T05:34:50.214Z","action":"created"}
{"ts":"2026-09-23T10:58:07.675Z","action":"updated:status"}
