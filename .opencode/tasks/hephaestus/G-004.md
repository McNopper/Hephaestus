---
id: G-004
title: Wire the tuning tables - no remaining magic numbers
type: task
status: done
priority: medium
role: developer
stage: null
story_points: 3
sprint: null
epic: null
assignee: null
blocked: false
blocker: null
labels: ["production-readiness"]
acceptance_criteria: ["See the description for the full scope"]
created_at: 2026-09-14T07:00:00.000Z
updated_at: 2026-09-16T07:18:59.160Z
---

ClientTuning/GitTuning declared but never referenced (launcher, event stream, drain waits still hard-code). TaskFleetLauncher.LAUNCH_TIMEOUT duplicates. GitCli bypasses GitLocator/GitTuning.

## Artifacts
{"kind":"path","ref":"eclipse/bundles/{com.opencode.ide.client,com.opencode.ide.client.tests,com.opencode.ide.git,com.opencode.ide.git.tests}","note":"Client+git bundle half of G-004: 6 files changed, all knobs routed with defaults identical to the previous hard-coded values (wiring, not retuning). Uncommitted in working tree per instructions; orchestrator verifies via central build.","by":"executor (client/git half)","ts":"2026-09-16T06:44:32.325Z"}
{"kind":"path","ref":"eclipse/bundles/com.opencode.ide.board/src/main/java/com/opencode/ide/board/{internal/GitCli.java,fleet/TaskFleetLauncher.java}","note":"Board-bundle half of G-004: GitCli timeout → GitTuning.COMMAND_TIMEOUT and stream-drain join → GitTuning.OUTPUT_DRAIN_WAIT; TaskFleetLauncher\u0027s LAUNCH_TIMEOUT alias deleted, both engine calls now pass FleetTuning.DEFAULT_TICKET_BUDGET directly (defaults identical to the previous hard-coded values — wiring, not retuning). Uncommitted per instructions; orchestrator verifies centrally.","by":"executor (board half)","ts":"2026-09-16T07:06:40.679Z"}

## Comments
{"ts":"2026-09-16T06:44:46.314Z","by":"executor (client/git half)","text":"COMPLETION REPORT — client + git bundle half (board bundle\u0027s GitCli/TaskFleetLauncher NOT touched, per split).\n\nKNOBS WIRED (all defaults \u003d previous hard-coded values; no behavior change):\n1. ClientTuning.DESTROY_WAIT (CLIENT_DESTROY_WAIT_MS, 3s) → OpencodeServerLauncher.stop() waitFor(3,SECONDS) [was declared, unused]\n2. ClientTuning.PROBE_INTERVAL (CLIENT_PROBE_INTERVAL_MS, 500ms) → OpencodeServerLauncher.waitForHealth() Thread.sleep(500) [declared, unused]\n3. ClientTuning.SSE_STABLE_CONNECTION (CLIENT_SSE_STABLE_MS, 10s) → OpencodeEventStream stability window (replaces local STABLE_CONNECTION_MILLIS\u003d10_000L) [was only misused as a connect timeout]\n4. ClientTuning.SSE_BACKOFF_BASE / SSE_BACKOFF_MAX (CLIENT_SSE_BACKOFF_BASE_MS 1s / _MAX_MS 30s) → OpencodeEventStream reconnect backoff, refactored to Duration with nextBackoff() clamp; doubling sequence 1,2,4,...,30 preserved [declared, unused]\n5. NEW ClientTuning.SSE_CONNECT_TIMEOUT (CLIENT_SSE_CONNECT_TIMEOUT_MS, 10s) → OpencodeEventStream HttpClient.connectTimeout — was wired to the semantically-wrong SSE_STABLE_CONNECTION knob (same 10s value, so defaults identical); now the stream\u0027s TCP connect budget is independent of the REST CONNECT_TIMEOUT\n6. ClientTuning.SNIPPET_MAX (CLIENT_SNIPPET_MAX, 500) → HttpOpencodeClient error-body truncation ×2 sites [declared, unused]\n7. ClientTuning.SNIPPET_MIN (CLIENT_SNIPPET_MIN, 120) → HttpOpencodeClient malformed-body warning truncation ×6 sites [declared, unused]\n8. GitTuning.OUTPUT_DRAIN_WAIT (GIT_DRAIN_WAIT_MS, 5s) → GitStore.join() + GitWorktreeManager.join() future.get(5,SECONDS), via DRAIN_WAIT field aliases matching each file\u0027s style [declared, unused]\n\nFILES CHANGED (6): ClientTuning.java, OpencodeServerLauncher.java, OpencodeEventStream.java, internal/HttpOpencodeClient.java (client bundle); internal/GitStore.java, internal/GitWorktreeManager.java (git bundle). No test changes needed: new knob uses the stock env parser (no non-trivial parsing) and no tuning-table tests exist to extend.\n\nDELIBERATELY LEFT HARD-CODED (documented in comments): HttpOpencodeClient truncate tiers 200/300 and OpencodeEventStream truncate(json,160) — intermediate diagnostic verbosity tiers between SNIPPET_MIN/MAX; knob-ifying them adds three knobs for log detail, not operational behavior (FleetTuning, the reference discipline, has no snippet knobs at all). GitProcesses.terminate()\u0027s unbounded waitFor() after destroyForcibly — a wait-until-reaped correctness measure with no numeric value to tune.\n\nVERIFICATION: full-bundle grep confirms Duration.of()/sleep(n)/waitFor(n)/future.get(n,) literals now exist ONLY inside ClientTuning/GitTuning. No public API changed. Maven/Tycho NOT run per instructions (orchestrator verifies centrally). Confidence: high — mechanical rewiring, every hunk re-read against the diff."}
{"ts":"2026-09-16T07:07:21.967Z","by":"executor (board half)","text":"COMPLETION REPORT — G-004, board-bundle bits (complements the client/git half already reported).\n\nWIRED (defaults identical to previous hard-coded values — wiring, not retuning):\n1. GitCli.TIMEOUT (was Duration.ofSeconds(60)) → GitTuning.COMMAND_TIMEOUT (exported package com.opencode.ide.git — no MANIFEST change needed; env GIT_COMMAND_TIMEOUT_MS).\n2. GitCli.join() drain wait (was future.get(5, SECONDS)) → GitTuning.OUTPUT_DRAIN_WAIT (same 5s default) — symmetric with the client/git half\u0027s GitStore/GitWorktreeManager joins.\n3. TaskFleetLauncher: the private LAUNCH_TIMEOUT alias (which already pointed at FleetTuning.DEFAULT_TICKET_BUDGET since 1cea970) is DELETED; both engine calls pass FleetTuning.DEFAULT_TICKET_BUDGET directly, so the fleet knob has exactly one name. Import added (matches the file\u0027s existing fleet imports).\n\nDELIBERATELY NOT DONE — GitLocator routing for GitCli\u0027s binary discovery: GitLocator lives in the git bundle\u0027s INTERNAL package (com.opencode.ide.git.internal), exported with x-friends:\u003d\"com.opencode.ide.git.tests\" only. Reusing it from the board bundle requires editing the GIT bundle\u0027s MANIFEST (extend x-friends) — outside this dispatch\u0027s file scope (board + board.tests only; the instruction\u0027s manifest exception covered only the GitTuning export case, which was already exported). Left as bare \"git\" PATH invocation (previous behavior), documented in GitCli\u0027s javadoc. If wanted: one-line x-friends extension in the git MANIFEST + swap List.of(\"git\", ...) for GitLocator.resolve().command().toString().\n\nTESTS: none needed/added — the routing is compile-time-visible constant wiring (no test asserted the old literals; GitCliTest\u0027s explicit-timeout run() tests are unaffected). Board-bundle grep confirms no Duration.of* literals remain in GitCli/TaskFleetLauncher outside the tuning references.\n\nNOT RUN: Maven/Tycho (orchestrator verifies centrally). Confidence: high — mechanical rewiring against exported, already-referenced constants."}

## History
{"ts":"2026-09-16T06:44:32.347Z","action":"artifact:path:eclipse/bundles/{com.opencode.ide.client,com.opencode.ide.client.tests,com.opencode.ide.git,com.opencode.ide.git.tests}","by":"executor (client/git half)"}
{"ts":"2026-09-16T07:06:40.679Z","action":"artifact:path:eclipse/bundles/com.opencode.ide.board/src/main/java/com/opencode/ide/board/{internal/GitCli.java,fleet/TaskFleetLauncher.java}","by":"executor (board half)"}
{"ts":"2026-09-16T07:18:59.160Z","action":"updated:status"}
