---
id: W-006
title: T: Navi golden-vector tests (EditorCoreTest.java)
type: task
status: sprint-backlog
priority: critical
role: tester
stage: test-implementation
story_points: 3
sprint: S-06
epic: null
assignee: null
blocked: true
blocker: worker produced no changes (no commits on opencode/W-006 and no pending worktree edits)
labels: ["regression","v-model","editor"]
acceptance_criteria: ["EditorCoreTest.java exists and runs green via `java EditorCoreTest.java` (exit 0) - the run output is pasted as a ticket comment","At least 15 golden vectors, every pinned binding covered, all six edge cases present","Each vector asserts the FULL resulting CoreState (lines, cursor, mode) plus undo behavior where applicable","No modification to EditorCore.java unless a vector exposed a genuine spec violation - any such fix is documented on the ticket","Only files under scratch/fleet-regression/editor/ are touched"]
created_at: 2026-08-28T03:25:20.578Z
updated_at: 2026-09-14T03:27:05.541Z
---

V-model regression stage 6 of 6. Implement scratch/fleet-regression/editor/EditorCoreTest.java: golden-vector tests against the pinned spec - at minimum 15 vectors covering every binding at least once plus the six edge cases (empty buffer, col clamping, cursor at EOL/EOF, Backspace at col 0 joining lines, Delete at EOL joining lines, PageDown clamping at buffer end; undo restores exact state incl. cursor). Plain Java, no test framework: main() runs all vectors, prints PASS/FAIL per vector, exits non-zero on any failure. RUN it with `java EditorCoreTest.java` and record the output on the ticket.

PINNED SPEC (verbatim, product-owner authored; the binding table is fixed - do not paraphrase):
Navi editor v1: two modes NORMAL and INSERT, startup NORMAL with empty buffer. Bindings: arrows move cursor (Left/Right chars, Up/Down lines, col clamps to line length); Home/End jump to line start/end; Ctrl+Home/Ctrl+End to buffer start/end; PageUp/PageDown move 12 lines; i or Insert enters INSERT at cursor; in INSERT printable characters insert at cursor, Enter inserts a newline after cursor line, Backspace deletes left char or joins previous line when col=0, Delete deletes right char or joins next line at EOL; Esc returns to NORMAL; in NORMAL x deletes char at cursor, d deletes current line, u undoes the last change (full-state snapshots, stack max 100), Ctrl+S saves the buffer to the loaded path (no-op when no path), Ctrl+Q quits. The core is a pure function apply(state, key) -> state; CoreState = {lines: string[], cursor: {line, col}, mode: NORMAL|INSERT, undoStack: CoreState[]} serialized as JSON. Work happens under scratch/fleet-regression/editor/ only.

## Comments
{"ts":"2026-09-14T03:26:03.758Z","by":"fleet","text":"launched into worktree opencode/W-006 by the fleet"}

## History
{"ts":"2026-08-28T03:25:20.578Z","action":"created"}
{"ts":"2026-08-28T03:25:21.286Z","action":"planned into S-06"}
{"ts":"2026-09-14T03:26:03.685Z","action":"updated:assignee,status"}
{"ts":"2026-09-14T03:27:05.433Z","action":"blocked:worker produced no changes (no commits on opencode/W-006 and no pending worktree edits)","by":"fleet"}
{"ts":"2026-09-14T03:27:05.541Z","action":"updated:assignee,status"}
