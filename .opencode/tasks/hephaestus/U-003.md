---
id: U-003
title: Chat streaming parity with the TUI: progressive markdown, inline Stop, message queueing
type: story
status: done
priority: medium
role: developer
stage: null
story_points: 5
sprint: sprint-2026-09-15
epic: E-004
assignee: null
blocked: false
blocker: null
labels: ["chat","ux","streaming"]
acceptance_criteria: ["While a reply streams, the bubble re-renders markdown incrementally (throttled, e.g. 4-10 renders/s), so tables/code/headings appear formatted during generation, not only after finalization; final authoritative render still replaces the body","A visible Stop control sits next to the input (or in the input row) while a reply is in flight, equivalent to the existing Abort action; the Ctrl+Alt+Shift+A binding keeps working","Messages typed while a reply is in flight are queued (shown as pending) and auto-sent when the reply completes - same behavior as the opencode TUI; queued items are editable/removable before sending","Queue state and abort are covered by component tests (fake renderer) - no SWT on the UI thread during streaming"]
created_at: 2026-09-15T03:42:51.536Z
updated_at: 2026-09-16T13:45:00.000Z
---

User direction 2026-09-15 (back in the TUI after live-testing the Eclipse chat): the Eclipse chat should match the TUI's streaming experience - colors, resolved markdown while streaming, message queueing and cancel. Current state: the chat page (eclipse/components/chat-web/web/chat.js) renders markdown/markdown tables, syntax-highlighted fences (hljs, theme-switched), KaTeX math and mermaid diagrams in FINAL renders only; during streaming the bubble shows raw monospace text (stream-raw) until __stopStream/__setAssistantText finalizes it - long generations stare at unformatted pipes/asterisks. The Abort action exists (toolbar + Ctrl+Alt+Shift+A, ChatView.abortRequested) but is undiscoverable - no inline Stop button. Message queueing does not exist: the input has a single Send path, no pending queue while generating. Scope: (1) progressive markdown rendering during streaming (render-on-delta, throttled; keep the stream-done guard semantics); (2) inline Stop control near the input while generating; (3) TUI-style queue: type+Enter while generating enqueues, pending messages visible/editable, auto-send on completion. Follow-up note: history loads render every bubble eagerly (a 1615-entry session rendered in one go) - consider lazy/virtualized history as a separate follow-up if it stays slow.

## Comments
{"ts":"2026-09-15T03:51:57.247Z","by":"fleet","text":"launched into worktree opencode/U-003 by the fleet"}
{"ts":"2026-09-15T04:00:56.766Z","by":"chat-session","text":"User emphasis (2026-09-15): \"the chat needs to stay responsive, so we need somehow a queue\" - the input must NEVER be disabled or frozen while a reply streams: keystrokes appear instantly, Enter enqueues (pending messages visible per the queue AC), and the SWT UI thread stays free during streaming (no jank from render floods - the throttled progressive rendering AC doubles as the jank guard). Treat input responsiveness as part of the definition of done for the queue feature."}
{"ts":"2026-09-15T04:06:45.784Z","by":"chat-session","text":"Refinement from the PO (2026-09-15, second pass): full markdown after complete receive is acceptable - the priority is BLOCK-LEVEL incremental rendering: as soon as a markdown block is complete during streaming (closed code fence, finished table/paragraph/list), render it as markdown immediately; only the trailing in-progress block stays raw with the cursor. Also surface thinking/reasoning while streaming (a \"thinking...\" state or streamed reasoning in the collapsible details), not only after finalization. Adjust the throttle AC accordingly: render cost scales with completed blocks, not whole-message re-renders."}

## History
{"ts":"2026-09-15T03:42:51.536Z","action":"created"}
{"ts":"2026-09-15T03:49:29.761Z","action":"planned into sprint-2026-09-15"}
{"ts":"2026-09-15T03:51:57.228Z","action":"updated:assignee,status"}
{"ts":"2026-09-15T03:52:56.015Z","action":"blocked:worker produced no changes (no commits on opencode/U-003 and no pending worktree edits)","by":"fleet"}
{"ts":"2026-09-15T03:52:56.064Z","action":"updated:assignee,status"}
{"ts":"2026-09-16T13:45:00.000Z","action":"done: all scope landed - block-level progressive markdown, inline Stop, TUI-style queue with edit/remove, reasoning streaming (6ae0e73 wave and later); tests in ChatSessionControllerTest + bridge-check.mjs","by":"chat-session"}
