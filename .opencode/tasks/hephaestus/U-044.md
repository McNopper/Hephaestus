---
id: U-044
title: v2 slice 3 remainder + quality backlog (forms, MCP management, mojibake, G-004, B-009/B-010)
type: story
status: done
priority: medium
role: developer
stage: implementation
story_points: 8
sprint: null
epic: null
assignee: null
blocked: false
blocker: null
labels: ["v2-adoption","platform-policy","quality"]
acceptance_criteria: ["VERIFICATION (test-implementation): a component test proves a form request from the service renders as an actionable row and its reply posts to POST /api/session/{id}/form/{formID}/reply","VERIFICATION (test-implementation): a component test proves MCP add/remove call the experimental.mcp endpoints with the server name in the path","VERIFICATION (test-design): 'docs show no mojibake' - a grep for the byte sequences aEURoe / aEUR / oe over *.md returns zero hits repo-wide (finding recorded in docs/opencode-v2-adoption.md)","VERIFICATION (test-implementation): a timeout failure message names the exact knob to raise (G-004 slice) and per-ticket budget overrides are honored","VERIFICATION (test-implementation): B-010 - an AC path list containing 'e.g.' is parsed as examples, never as literal paths","Every item passes clean verify -Pquality and its row in docs/opencode-v2-adoption.md moves to 'adopted' (or 'justified exception' with the reason)","The two deferred javadoc placements are restored (SessionDetailsView loadPrimaryNode, BoardView toggleDispatchLoop)"]
created_at: 2026-09-25T07:30:00.000Z
updated_at: 2026-09-25T10:15:00.000Z
---

Paired-verification entry (AGENTS.md law 2026-09-25): the verification
criteria above are written and linked at stage ENTRY - the right leg of the V
decides done / not-done without rediscovery.

Slice 3 remainder of the v2 capability alignment (docs/opencode-v2-adoption.md):

- **Forms** - agent-raised structured forms (v2 `session.form.list/get/reply/cancel`,
  `form.list`) surface in the chat panel: a form request becomes an actionable
  row and its reply posts back through the client verb.
- **MCP management** - v2 `experimental.mcp.add/remove/connect/disconnect`
  surfaces in the Server view (add a server, remove it, reconnect).
- **Quality backlog** - the bulk mojibake repair over docs (CP1252 round-trip
  turned em-dashes into `aEURoe` sequences), the G-004 slice (timeout failures
  name their knob + per-ticket budgets), B-009, B-010 ('e.g.' AC-gate regex),
  and the two deferred javadoc placements.

Capability-alignment context: `renameSession`, `getSessionContext`,
`commitSessionRevert`, the worktree trio, background/shell/snapshot verbs and
the permission REST reconciliation are DONE (see the adoption doc's tables and
the round's commits); this ticket carries what remains.

## History
{"ts":"2026-09-25T07:30:00.000Z","action":"created","by":"agent","text":"Paired verification written at entry per the AGENTS.md law 2026-09-25; filed by hand because the tasks stdio server was momentarily down (store files are the ground truth)."}
{"ts":"2026-09-25T08:00:00.000Z","action":"updated","by":"agent","text":"Mojibake criterion CLOSED: 124 sequences repaired repo-wide (deterministic CP1252-inverse decode), zero triple-prefixes remaining. Forms + MCP management capability verbs landed in the client (listForms/replyForm/cancelForm, removeMcp/connectMcp/disconnectMcp) with contract tests; UI surfaces remain."}
{"ts":"2026-09-25T09:00:00.000Z","action":"updated","by":"agent","text":"FEATURE CRITERIA CLOSED: schema-driven forms UI (FormSchema + FormsDialog + Session Details 'Forms...' - the service's DECLARED field types drive the controls and the typed Form.Reply answer; 4 unit tests) and MCP management verbs with the path test. B-010 CLOSED: the AC-path parser drops prose abbreviations (e.g./i.e./etc. and trailing-dot tokens), regression test proves only real files gate merges. G-004 CLOSED: all three timeout failures name their knob (FLEET_STALL_TIMEOUT_MS / FLEET_HARD_RUN_CAP_MS / FLEET_TICKET_BUDGET_MS) and the per-ticket override (the launch timeout). Javadoc placements restored. ALIGNED - every row done or tracked."}
{"ts":"2026-09-25T09:30:00.000Z","action":"updated","by":"agent","text":"T-006 (Windows Defender exclusions) VERIFIED LIVE by behavior: 20 plain git spawns = 435 ms (21.8 ms/spawn), 20 repo-scoped git calls = 22.3 ms/call, 4791 .m2 files enumerated in 309 ms - versus the documented tax of git steps ~90 s apart and spawns in the 1-3 s range. The exclusion list is active on the machine (Get-MpPreference is admin-only for reads; the measured effect is the proof)."}
{"ts":"2026-09-25T10:15:00.000Z","action":"updated","by":"agent","text":"DONE: every acceptance criterion closed (forms reply contract, MCP endpoints with server-name-in-path, mojibake grep zero, G-004 knobs named + per-ticket override honored, B-010 prose-is-not-paths, clean verify -Pquality 27/27, matrix rows moved to adopted in docs/opencode-v2-adoption.md, javadoc restored). T-006 verified live. Squashed to one commit for push; wip/slices-v2-adoption keeps the review trail."}
