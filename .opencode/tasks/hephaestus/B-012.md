---
id: B-012
title: stdio MCP pipes die with "Connection closed" while their JVM stays alive
type: bug
status: product-backlog
priority: high
role: developer
stage: null
model: null
story_points: 3
sprint: null
epic: null
assignee: null
blocked: false
blocker: null
labels: ["mcp","reliability"]
acceptance_criteria: ["Root cause identified (service-side vs launcher-side)","A dead stdio pipe fails fast and respawns instead of lingering as a zombie JVM","Regression coverage where feasible"]
created_at: 2026-09-22T04:09:06.595Z
updated_at: 2026-09-23T05:34:50.048Z
---

Three occurrences in two days (2026-09-21 x2 tasks+fleet, 2026-09-22 tasks): the tasks/fleet stdio MCP servers report Connection closed and vanish from the tool catalog while the launcher JVM process is still running. Recovery works via POST /api/experimental/mcp/{server}/connect. Suspected: stdin/stdout pipe breaks (service restart, config reload, or jar replacement under the running JVM). Find the trigger; at minimum detect the dead pipe in the launcher and exit nonzero so opencode respawns instead of showing a zombie.

## Comments
{"ts":"2026-09-23T05:34:50.048Z","by":"kimi-k3","text":"Root cause identified (AC1): service-side TRIGGER (service restart/config reload/jar swap - the three occurrences correlate), launcher-side ZOMBIE: java.io.PrintStream swallows broken-pipe IOExceptions so writes look fine, a half-dead pipe never EOFs the blocked readLine, and Windows does not cascade a launcher kill to the JVM. Fix: shared StdioServer (tools bundle) - daemon reader thread + control loop (no unreliable close(System.in)), three liveness signals: PrintStream.checkError after every flushed write, stdin read failure, ParentLiveness (snapshotted parent handle + startInstant, PID-reuse-safe; empty parent \u003d watch disabled). Exit codes: 0 clean EOF, 2 pipe died, 3 orphaned - nonzero so the host respawns (tasks-tools.ps1/fleet-tools.ps1 already forward $LASTEXITCODE). FleetStdioMain\u0027s shutdown hook still kills the spawned serve on every exit path. Regression: StdioServerTest x6 (clean EOF, broken stdout fail-fast, read failure, orphan, orphan+read failure, notification silence)."}

## History
{"ts":"2026-09-22T04:09:06.595Z","action":"created"}
