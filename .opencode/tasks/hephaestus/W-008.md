---
id: W-008
title: "I3: Navi editing + undo transitions (EditorCore.apply part 2)"
type: task
status: sprint-backlog
priority: critical
role: general
stage: implementation
story_points: 3
sprint: S-06
epic: null
assignee: null
blocked: false
blocker: null
labels: ["regression","v-model","editor"]
acceptance_criteria: ["EditorCore.java still compiles (javac, JDK 21)","apply() handles: printable chars in INSERT, Enter, Backspace (join at col 0), Delete (join at EOL), x, d, u (undo)","Undo: full-state snapshots pushed before each MUTATING transition, stack max 100 (oldest dropped)","Every edit respects the mode gates (INSERT keys no-op in NORMAL except i/Insert; NORMAL keys no-op in INSERT except Escape)","main() smoke extended: type 'hello', Enter, 'world', Backspace, x, u - each state printed as JSON"]
created_at: 2026-09-14T05:36:00.000Z
updated_at: 2026-09-14T05:36:00.000Z
---

DECOMPOSED implementation chunk 3 of 3. Chunks 1 (JSON codec) and 2 (cursor+
mode) must be MERGED first — this ticket builds on `EditorCore.apply` with
the cursor/mode table already in place.

**FIRST STEP:** open the existing `EditorCore.java`, locate the `apply`
method, add the editing cases below alongside the existing cursor cases,
compile.

**THE EDITING TRANSITIONS (self-contained — do NOT read DESIGN.md):**

| Key | Mode | Effect |
|---|---|---|
| any single printable char (length 1, not a special key) | INSERT | insert at cursor; col+1 |
| `"Enter"` | INSERT | split the line at cursor; cursor to new line, col=0 |
| `"Backspace"` | INSERT | col>0: delete char left of cursor, col-1. col==0 && line>0: join with previous line (cursor at join point) |
| `"Delete"` | INSERT | col<line length: delete char right of cursor. col==line length && line<last: join next line onto this one |
| `"x"` | NORMAL | delete char at cursor (if line non-empty); col clamps |
| `"d"` | NORMAL | delete the entire current line (if it leaves >= 1 line; an empty buffer keeps one empty line); cursor clamps |
| `"u"` | NORMAL | pop the newest snapshot from undoStack, restore it as the state |

**Undo protocol:** before EVERY state-changing transition (the ones above +
mode changes from chunk 2 are NOT undoable; only buffer edits are), push a
full deep-copy snapshot of the pre-edit state onto `undoStack` (append;
if length would exceed 100, drop the OLDEST). `"u"` pops the LAST snapshot
and restores it — the restore itself does not push a new snapshot.

**Write scope:** exactly `scratch/fleet-regression/editor/EditorCore.java`.
Modifying that file IS the task — a run that only analyzes is a FAILED run.
